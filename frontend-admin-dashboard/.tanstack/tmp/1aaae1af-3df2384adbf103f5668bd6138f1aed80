import { createFileRoute } from "@tanstack/react-router"

export const Route = createFileRoute("/admission/document-verification")({
  component: RouteComponent,
})

function RouteComponent() {
  return <div>Hello "/admission/document-verification"!</div>
}
